<% if(errors){ %>
    <% errors.forEach(function(error){ %><div class="error_message"> <%= error %> </div><% }) %>
<% } %>

<% errors.forEach(function(error){ %>
    <div class="error_message">
        <span><%= error.msg %></span>
    </div>
<% }) %>